"use client";

// Public pages are presentation-only. All content editing is handled in the admin workspace.
export function EditableSection(_props: { label?: string; footer?: boolean }) {
  void _props;
  return null;
}
